#!/bin/sh
# Strip co-author and metadata lines from commit messages
if [ -f "$1" ]; then
  sed -i '' -e '/Co-authored-by:/d' -e '/Made-with:/d' "$1" 2>/dev/null || \
  sed -i -e '/Co-authored-by:/d' -e '/Made-with:/d' "$1" 2>/dev/null
fi
